As funções que preciso:

checa_venceu(char, char**) : checa se o char passado é o vencedor do
jogo. Retorna zero se venceu, diferente de zero se não.

checa_velha(char** ): checa o jogo pra ver se deu velha. Retorna zero se
deu, 1 se não deu, 2 se impossível determinar.
 
 Essa é foda! Como verificar isso? 
 1a ideia: copiar o jogo passado, e tentar jogar ate o fim, sozinho. Não
 é tão ruim, pois se essa função for chamada, é porque há dúvida num
 jogo existente se vai dar velha. Em outra ocasião, como a trivial do
 tabuleiro vazio, simplesmente diz que não é possível determinar
 (retorna 2?).

altera_estado(int, int, char, char**): retorna zero se a alteração
requerida for válida. Senão, retorna diferente de zero.

faz_jogada(char*, char**): o cérebro do progama é esse. Aqui está o
algoritmo que verifica, em teoria, a melhor jogada e a faz. Ele é
responsável por jogar em tabuleiro-cópia, para verificar se é possível
fazer uma jogada. Retorna 0 se a jogada foi bem sucedida e se niguem
ganhou, 1 se eu ganhei e 2 se fiz dar velha. *** CUIDAR DO \0 ****


